# CADE - 具身智能机器人系统依赖
# Python 版本要求: >=3.10

# ==================== 核心框架 & 环境变量 ====================
openai>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0     # 必须添加，用于读取 .env
httpx>=0.24.0            # 你的代码中用于禁用代理逻辑

# ==================== 异步支持 ====================
aiohttp>=3.9.0

# ==================== 日志和调试 ====================
loguru>=0.7.0

# ==================== 音频处理 ====================
sounddevice>=0.4.6       # 音频输入输出
soundfile>=0.12.1        # WAV 文件读写
numpy>=1.24.0            # 数值计算

# ==================== ASR/TTS 引擎 ====================
sherpa-onnx>=1.10.0      # 语音识别与合成

# ==================== ROS 桥接 ====================
# 在 Conda 环境中，必须 pip 安装才能识别
rospkg>=1.5.0
catkin_pkg>=0.5.0

# ==================== 状态机 ====================
transitions>=0.9.0
